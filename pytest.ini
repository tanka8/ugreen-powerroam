[pytest]
# No Home Assistant needed - tests/loader.py imports api.py directly, bypassing
# the package __init__ (which does import homeassistant). Runs on any platform.
testpaths = tests
pythonpath = .
